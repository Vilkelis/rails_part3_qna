$('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>');
$('.flash').html('<%= j render 'shared/flash' %>');
<% if @answer.persisted? %>
  $('.answers-list').append('<%= j render @answer %>');
  $('form#new_answer_form > #answer_body').val('');
<% end %>